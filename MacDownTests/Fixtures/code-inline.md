# Inline Code Tests

## Basic Inline Code

Use the `printf()` function to print output.

The variable `userName` stores the user's name.

## Multiple Code Spans

Use `git add` and then `git commit` to save changes.

## Code with Special Characters

This is `code with spaces`.

This contains `<html>` tags.

This has `back\`ticks` escaped.

Use `**not bold**` inside code.

## Edge Cases

`Code at start` of line.

End of line has `code`.

A paragraph with `multiple` inline `code` spans throughout.

Empty code: ``.

Code with symbols: `$PATH`, `@user`, `#define`.
